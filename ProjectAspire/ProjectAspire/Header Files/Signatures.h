#pragma once

#include <cstdint>

namespace Offsets
{
	inline uint8_t* getSequenceActivity = nullptr; // xref: client.dll | "No model!" | 1 Function below.
	inline uint8_t* hasC4 = nullptr; // xref: client.dll | "weapon_c4" | Same function.
	inline uint8_t* viewMatrix = nullptr; // xref: Todo.
	inline uint8_t* isHLTV = nullptr; // xref: client.dll | "mp_radar_showall" | Scroll up | test al, al.
}